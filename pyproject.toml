[project]
name = "simple-agent"
version = "0.1.0"
description = "Simple Weather & Time Agent using Google ADK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-cloud-aiplatform[agent_engines,adk]",
    "pydantic",
    "cloudpickle==3.0",
]
